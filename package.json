{
  "name": "ratio-css",
  "version": "0.2.0",
  "type": "module",
  "description": "Geometric ratio-based CSS utility framework. Two variables. Every dimension. Pure math.",
  "main": "ratio.css",
  "scripts": {
    "generate": "bun run src/generate.ts",
    "dev": "bun run server.ts",
    "start": "bun run server.ts",
    "build": "bun run src/generate.ts && cp ratio.css public/ && cp ratio.min.css public/"
  },
  "dependencies": {
    "elysia": "^1.4.0",
    "@elysiajs/static": "^1.4.0",
    "@elysiajs/swagger": "^1.3.0"
  },
  "devDependencies": {
    "playwright": "^1.58.2",
    "typescript": "^5.3.0"
  }
}
